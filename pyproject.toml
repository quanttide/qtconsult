[project]
name = "qtconsult"
version = "0.1.0"
description = "量潮咨询"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.34",
    "fastapi>=0.115",
    "httpx>=0.27",
    "playwright>=1.59.0",
    "pydantic>=2",
    "pytesseract>=0.3.13",
    "uvicorn[standard]>=0.34",
]

[dependency-groups]
dev = [
    "playwright>=1.52",
    "pytest>=8",
    "pytesseract>=0.3",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
