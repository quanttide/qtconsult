[project]
name = "qtconsult"
version = "0.0.2"
description = "量潮咨询"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
